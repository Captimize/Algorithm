#include <iostream>
using namespace std;
int N,m,M,T,R;


int main(void) {
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin >> N >>m >>M >>T >>R;
	int t;
	for (t = 1; t <= N;) {
		if (m + T <= M) {

		}
	}
}