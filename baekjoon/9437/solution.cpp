#include <iostream>
#include <vector>
using namespace std;
int N, P;
struct page {
	int a, b, c, d;
};
int main(void) {
	ios_base::sync_with_stdio(0); cin.tie(0);
	while (1) {
		cin >> N;
		if (N == 0) break;
		
	}
}